abstract final class AppConfig {
  static const appName = 'Tavola';
  static const defaultLocale = 'en_IN';
  static const apiTimeout = Duration(seconds: 30);
  static const pageTransitionDuration = Duration(milliseconds: 220);
}
